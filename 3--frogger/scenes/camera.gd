extends Camera2D


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass
	#limit_top = 0
	#limit_left = 0
	#var background = get_tree().get_first_node_in_group("background")
	#var background_size = background.get_rect().size
	#limit_right = background_size.x
	#limit_bottom = background_size.y
